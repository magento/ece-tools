stage:
  deploy:
    DATABASE_CONFIGURATION:
      _merge: true
      connection:
        default:
          host: localhost
          port: '3307'
          password: 1234
          user: user
    LOCK_PROVIDER: db
