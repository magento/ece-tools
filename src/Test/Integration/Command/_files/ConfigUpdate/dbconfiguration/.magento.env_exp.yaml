stage:
  deploy:
    DATABASE_CONFIGURATION:
      _merge: true
      connection:
        default:
          host: 127.0.0.1
          port: '3306'
          password: newpassword
          user: newuser
    LOCK_PROVIDER: redis
